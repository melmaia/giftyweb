import NavBar from "@/components/NavBar";

export default function Listas() {
  return ( 
    <main className="flex min-h-screen flex-col items-center">
      <NavBar active="listas" />

      <h1 style={{ color: 'black' }}>Pagina designada paras as listas</h1>

    </main>

    
  );
}
